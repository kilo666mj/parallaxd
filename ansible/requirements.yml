---
# ansible-galaxy collection install -r requirements.yml
collections:
  # firewalld source rules are defense in depth around the encrypted overlay,
  # so this remains a hard dependency rather than a convenience.
  - name: ansible.posix
